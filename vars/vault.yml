---
ssh_user_secret: "james"   # SSH user name
ssh_pass_secret: "RunStr8@1!"   # SSH pass you use to connect 
ssh_port_secret: "2002"   # IF running play on server with different SSH port than default 22 or IF running second time - change port here
target_server_ip_secret: "192.168.0.24"   # Address of server you want to target (provision) with the playbook
ssh_cert_secret: "/home/james/.ssh/id_rsa"    # #Point to private file used to connect
ssh_key_to_deploy_secret: "/home/james/.ssh/id_rsa.pub" #key to deploy for user